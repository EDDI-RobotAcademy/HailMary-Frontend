name: Deploy Frontend STAGING to S3 + CloudFront

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: frontend-deploy-staging
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: [self-hosted, hailmary-frontend]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build static export (staging env)
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.STAGING_API_URL }}
          NEXT_PUBLIC_QA_GATE_ENABLED: ${{ secrets.NEXT_PUBLIC_QA_GATE_ENABLED }}
          NEXT_PUBLIC_AMPLITUDE_API_KEY: ${{ secrets.STAGING_AMPLITUDE_API_KEY }}
          NEXT_PUBLIC_KAKAO_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_KAKAO_CLIENT_ID }}
          NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_GOOGLE_CLIENT_ID }}
          # 카드사 심사용 테스트 로그인 버튼 노출 (HM-FE-136). 심사 종료 후 secret=false 재배포로 숨김.
          NEXT_PUBLIC_TEST_LOGIN_ENABLED: ${{ secrets.NEXT_PUBLIC_TEST_LOGIN_ENABLED }}
          # 이미지 최적화 콘텐츠 해시 캐시 (HM-FE-104) — staging/prod 공유 EC2 영구 경로.
          IMAGE_CACHE_DIR: /home/ec2-user/.kkebi-image-cache

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      - name: Sync assets to staging S3 (long cache)
        run: |
          aws s3 sync out/ s3://${{ secrets.STAGING_S3_BUCKET }} \
            --delete \
            --cache-control "public, max-age=31536000, immutable" \
            --exclude "*.html" --exclude "*.txt" --exclude "*.json"

      - name: Sync HTML to staging S3 (no cache)
        run: |
          aws s3 sync out/ s3://${{ secrets.STAGING_S3_BUCKET }} \
            --delete \
            --cache-control "public, max-age=0, must-revalidate" \
            --exclude "*" --include "*.html" --include "*.txt" --include "*.json"

      - name: Invalidate staging CloudFront
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.STAGING_CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"
